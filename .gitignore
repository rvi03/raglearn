# Local docs (PRD, design notes — kept out of version control)
.docs/

# JetBrains IDE
.idea/

# Claude working rules (kept local)
CLAUDE.md

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Env / secrets
.env

# Local data (DuckDB files, downloaded filings, page images)
/data/
*.duckdb
*.duckdb.wal

# Integration tests requiring a live Postgres (kept local, not committed)
backend/tests/test_stores_postgres_chat.py
backend/tests/test_stores_postgres_structured.py

