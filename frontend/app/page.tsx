import type { ReactNode } from "react";

export default function HomePage(): ReactNode {
  return (
    <div className="placeholder">
      <h1>finrag</h1>
      <p>Select a view from the left — Documents or Chat.</p>
    </div>
  );
}
